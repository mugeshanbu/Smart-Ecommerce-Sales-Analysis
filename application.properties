# Server settings
server.port=${PORT:8080}

# MongoDB settings
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://mugesh:mugesh%400909@cluster0.2t3uidb.mongodb.net/bi_platform?retryWrites=true&w=majority&appName=Cluster0}

# File Upload properties
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Mail properties
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:user@example.com}
spring.mail.password=${MAIL_PASSWORD:password}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_STARTTLS_ENABLE:true}

# JWT configuration
app.jwt.secret=${JWT_SECRET:secure_secret_key_9876543210_smart_shop_bi_platform_enterprise}

# CORS configuration
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:*}
